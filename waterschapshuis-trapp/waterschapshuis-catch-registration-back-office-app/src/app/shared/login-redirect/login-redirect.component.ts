import { Component } from '@angular/core';

@Component({
    templateUrl: './login-redirect.component.html'
})
export class LoginRedirectComponent {
    // landing page after being redirected from Azure AD after login
}
